name: Demo Reset

on:
  schedule:
    - cron: "0 4 * * *"
  workflow_dispatch:

jobs:
  reset:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: superfly/flyctl-actions/setup-flyctl@master

      - name: Stop running machines
        run: |
          for id in $(flyctl machine list -a opensauce --json | jq -r '.[].id'); do
            flyctl machine stop "$id" -a opensauce || true
          done
          sleep 10
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}

      - name: Reset demo database
        run: |
          MACHINE_ID=$(flyctl machine list -a opensauce --json | jq -r '.[0].id')
          flyctl machine start "$MACHINE_ID" -a opensauce
          sleep 15
          flyctl ssh console -a opensauce -C "sh -c 'unset PHX_SERVER && SEED_DATA=true bin/opensauce eval OpenSauce.Release.reset'"
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}

      - name: Redeploy to clean restart
        run: flyctl deploy --image ghcr.io/puemos/opensauce:latest -a opensauce
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
