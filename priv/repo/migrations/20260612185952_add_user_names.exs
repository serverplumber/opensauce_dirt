defmodule OpenSauce.Repo.Migrations.AddUserNames do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    alter table(:accounts_users) do
      add :first_name, :text
      add :last_name, :text
    end
  end

  def down do
    alter table(:accounts_users) do
      remove :last_name
      remove :first_name
    end
  end
end
